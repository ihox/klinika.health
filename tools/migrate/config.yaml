# Klinika migration tool configuration.
# Concrete table mappings land in a later slice (see docs/data-migration.md).

source:
  path: ~/PEDIATRIA.accdb

target:
  dsn: ${DATABASE_URL}

options:
  dry_run: true
  batch_size: 500
